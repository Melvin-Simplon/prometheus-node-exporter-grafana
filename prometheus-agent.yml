global:
  scrape_interval: 5s
  external_labels:
    replica: agent

scrape_configs:
  - job_name: "node-exporter"
    static_configs:
      - targets: ["node-exporter:9100"]

remote_write:
  - url: "http://prometheus-server:9090/api/v1/write"